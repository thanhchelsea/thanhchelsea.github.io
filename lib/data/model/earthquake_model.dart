class EarthquakeModel {
  String address;
  double magnitude;
  double lat, lng;
  String time;
  double depth;
  int riskLevel;

  EarthquakeModel({this.address, this.magnitude, this.lat, this.lng, this.time, this.depth, this.riskLevel});
}
